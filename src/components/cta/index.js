import React from "react";
import PropTypes from "prop-types";

/** ***************************************************************************
 *  Call-to-Action Component
 *  A CTA may be linked to internal or external pages
 *************************************************************************** */
const CTA = props => {
  console.log(props);
  return <div>This where the CTA goes</div>;
};

CTA.propTypes = {};

export default CTA;
